{
  "name": "urlshortner",
  "version": "1.0.0",
  "description": "This is a url shortening app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "client": "cd ../client && npm install && npm start",
    "install-client": "cd ../client && npm install",
    "build": "cd ../client && npm run build",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.986.0",
    "bcrypt": "^5.0.1",
    "connect-mongo": "^4.5.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-session": "^1.17.2",
    "mongoose": "^6.0.4",
    "mongoose-type-url": "^2.1.0",
    "multer": "^1.4.3",
    "nodemailer": "^6.6.3",
    "nodemon": "^2.0.12",
    "shortid": "^2.2.16",
    "uuid": "^8.3.2",
    "validator": "^13.6.0"
  }
}
